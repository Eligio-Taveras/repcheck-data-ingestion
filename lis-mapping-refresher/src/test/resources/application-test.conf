# Test configuration overrides for LIS Mapping Refresher

database {
  host = "localhost"
  port = 5432
  database = "repcheck_test"
  username = "test"
  password = "test"
  max-connections = 2
}

pipeline {
  parallelism = 1
  request-timeout = 5s
}
