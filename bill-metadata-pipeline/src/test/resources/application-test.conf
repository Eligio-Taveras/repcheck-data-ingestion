# Test configuration overrides for Bill Metadata Pipeline

database {
  host = "localhost"
  port = 5432
  database = "repcheck_test"
  username = "test"
  password = "test"
  max-connections = 2
}

congress-api {
  base-url = "http://localhost:8080"
  api-key = "test-api-key"
  page-delay = 0ms
  retry {
    max-retries = 0
    initial-backoff-ms = 1
    max-backoff-ms = 10
    backoff-multiplier = 1.0
  }
}

pipeline {
  lookback-days = 7
  parallelism = 1
}
